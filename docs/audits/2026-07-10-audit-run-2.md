# Terminology/Structure Audit — Run #2 (2026-07-10)

Status: evidence document. Verdicts recorded in `term-inventory.json` /
`doc-registry.json`; nothing here changes canon by itself. Procedure:
`HARVEST.md`.

## Headline

Run #1's recommendations were almost entirely already actioned in the P3
cleanup batch (renames, ghost registrations, alias additions — see
`docs/SYNC-DEBT.md` and commit `4c83432`) before this run started. Run #2's
job was narrower than a first harvest: regenerate the two baselines,
reconcile drift since run #1, and close the two items P3 had not touched.
**Net: 222 → 221 terms** (1 genuine drop, not 3 — see the correction
below), **107 → 117 registry rows**, `npm run lint:docs` clean (8 checks,
0 findings), full suite 207/207.

## Correction to run #1's own verdict (self-audit finding)

Run #1 marked 3 rows `undetermined` ("ruling-statements harvested as
terms; drop from inventory"). Acting on that instruction literally and
dropping all 3 immediately regressed `lint:docs` with 2 fresh
`unregistered-definition` findings: **"Full adjacency, no neutral zones"**
and **"Mature-state start"** are not ruling-statement noise — they are
real Tier-0 `DOMAIN_MAP.md` rows (`✅`/`❓` markers, match-arc §"Match
structure") promoted from match-arc `GLOSSARY.md`'s "Frame decisions"
section. Run #1's classification was wrong for 2 of its 3 "undetermined"
rows. Restored both with `verdict: null` (never actually judged against
the genre/theory dictionaries — queued for the next Ring B pass per
`HARVEST.md` step 6, rather than re-guessing a verdict here).

The third, **"Wall grade is public information"**, checks out as
originally classified: it is a `RULINGS.md` section title (`## ① Wall
grade is public information — SEALED ...`), never promoted to a
GLOSSARY/DOMAIN_MAP row. Correctly dropped.

Lesson for the next run: cross-check a birthplace-surface finding against
**all** definition surfaces (DOMAIN_MAP promotion included) before acting
on a drop recommendation, not just the feature GLOSSARY the row happens to
cite as birthplace. `HARVEST.md` step 2 amended with this map-lore/
undetermined disambiguation (see below).

## Map-lore exemption, corrected

Run #1's report said to "exclude map-lore proper nouns... from future
audit runs." Read literally this means the same thing it almost meant for
the undetermined rows: dropping Great range / Hexi corridor / Western Ring
/ Taishan from the inventory would immediately false-positive
`checkHeaderDiff`, since all four are real `| Term |` table rows in
`terrain-cradle/GLOSSARY.md`. Kept their inventory rows. What actually
changes: **`HARVEST.md` now says explicitly** that map-lore proper nouns
are exempt from the dual-dictionary verdict-judging pass (Ring B) — they
are place names, not mechanic/meta names, so genre/theory-dictionary
routing does not apply to them. The index row stays; only the judgment
step is skipped going forward.

## Gaps closed

- **Province archetype region aliases**: run #1 flagged 4 of ADR 0006's 12
  named archetype regions as missing aliases (Northeastern Frontier,
  Southeastern Coast and Straits, Northwestern Oasis, Desert Corridor).
  Checked the full ADR 0006 list and found 7 missing, not 4 (the report's
  4 were the ones that also recur in `DESIGN.md`; three ADR-only names —
  Hebei and Northern Plains, Han/Jing River Corridor, Southwestern Basin,
  Southern Mountain and Forest Frontier — weren't flagged but were equally
  absent). Added all 7; the row now carries the complete 12-name set as
  aliases. Note "Northwestern Oasis and Desert Corridor" is one compound
  archetype name in ADR 0006, not two — added as written in the source.

## Ring A re-sweep: RULINGS.md / model docs / QUICKREF

`checkHeaderDiff` only scans `DOMAIN_MAP.md` + feature `GLOSSARY.md`
files. `HARVEST.md`'s Ring A also names `RULINGS.md` (coined-but-
unregistered terms), model docs (MAGNITUDE/FORMULA/MATCHUP/CATALOG/
STRATEGY-SPACE/TEST-LADDER/BATTERY), and `GLOSSARY-QUICKREF.md` — none of
which lint touches. Extracted all bolded phrases across 13 such files (398
candidates) and diffed against the 222-term index. Result: no genuine
unregistered domain terms. The residue is entirely prose emphasis, ruling
labels (`RULING ①`, `G14`, ...), formula fragments (`f₀ = 0.5`, `× 0.5
both ways`), and analysis-doc vocabulary that does not meet `HARVEST.md`
step 2's term bar. Two candidates worth a one-line note for a future pass
if a second consumer ever needs them (not registered — no current driving
need, per the promotion rule):

- **Crown inversion** (match-arc `RULINGS.md`) — explicitly "recorded as
  evidence, NOT a rewrite"; an empirical finding, not a sealed term.
- The 5 `BATTERY.md` "value" comparisons (Diversity/Judgment/Information/
  Disposition value, Freeze absorption) — statistical-comparison labels
  for one measurement battery, not game-mechanic names.

Conclusion: **no ghost-term drift since run #1.** The extended-surface
sweep is a real check, not a rubber stamp — it just came back negative
this time.

## doc-registry.json reconciliation

Diffed the 107 registered paths against the actual governed tree
(SPEC/DESIGN/DOMAIN_MAP, `docs/adr/`, `docs/features/*/`, `docs/audits/`,
`docs/superpowers/`, `docs/teach/`, `mockup/*/NOTES.md`). Zero dead rows
(`checkBaselineSelf` already covers that continuously). 10 files existed
but were never registered — all created after run #1's snapshot:

| Path | Layer |
|---|---|
| `docs/adr/0030-...hegemony-and-domination.md` | record |
| `docs/adr/0031-...force-geography-defense-model.md` | record |
| `docs/audits/2026-07-10-structure-forensics.md` | working |
| `docs/audits/2026-07-10-terminology-audit.md` | working |
| `docs/audits/HARVEST.md` | working |
| `docs/features/fog-of-war-discovery/GLOSSARY.md` | production |
| `docs/superpowers/plans/2026-07-10-doc-governance-p3-p1-implementation.md` | working |
| `docs/teach/assets/lesson.css` | sanctuary |
| `docs/teach/assets/quiz.js` | sanctuary |
| `docs/teach/lessons/0001-four-layer-stack-model.html` | sanctuary |

Added all 10, using the nearest existing sibling row as the role-text
template (e.g. ADR 0028 for the two new ADRs, `terrain-cradle/GLOSSARY.md`
for the new fog GLOSSARY).

## What this run deliberately left alone

- `actionCapacity`↔`capacity`, `computeProvinceStatus`↔`classifyHex` code-
  contract drift — still folded into the owed `js/situation.js` rework
  (SYNC-DEBT), not a re-harvest action.
- `gold`→treasury prose leak — `SPEC.md:264` fixed this session (prior to
  this run) rewritten to "treasury yield"; `ADR 0013:33` checked and
  confirmed a false positive (names a map-legend color, not the resource).
- The remaining 5-item rename agenda entries that got aliases-only in P3
  (not full canonical renames) are treated as P3's final disposition, not
  reopened here — that was a user-adjacent decision pass, out of scope for
  a mechanical re-harvest.

## Next (SYNC-DEBT promotion chain)

(b) `/doc-audit` skill codification — gate: one natural session-close
`lint:docs` run. This session did substantive doc-governance edits
(residue fix + this audit run); running `npm run lint:docs` at session
close satisfies that gate. (c) hook promotion — same gate, same session.
